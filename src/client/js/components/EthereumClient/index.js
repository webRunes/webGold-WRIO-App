import HookedWeb3Provider from 'hooked-web3-provider'
//import Accounts from 'ethereumjs-accounts'
import web3 from 'web3'
import {Promise} from 'es6-promise'
import React from 'react'

class EthereumAccount extends React.Component{
/*
    constructor(props) {
        super(props);
        console.log("Accounts initialization");

        this.accounts = new Accounts({minPassphraseLength: 6});
        window.accounts = accounts; // For debugging.
        var provider = new HookedWeb3Provider({
            host: "http://localhost:8545",
            transaction_signer: this.accounts // transaction_signer must implement signTransaction() and hasAddress()
        });
        web3.setProvider(provider);

    }

    createAccount (pass) {

        var pass = this.accounts.new(pass);



    }

    render() {
        return (
            <div className="callout">
                <h5>Ethereum status</h5>
                <p>WRG ballance:</p>
                <p>Ethereum ballance:</p>
            </div>
        );
    }
*/
}


