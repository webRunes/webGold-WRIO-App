/**
 * Created by michbil on 08.10.15.
 */
/*try {
    require("babel/register")({
        stage: 0
    });
} catch (e) {
    console.log("Babel polyfill already loaded, seems to be ok, relaxing....");
}*/

import app from './src'

module.exports = app;